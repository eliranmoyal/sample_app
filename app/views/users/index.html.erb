<h1>All Users</h1>

<%= will_paginate @users %>
<ul class="users">
	<% @users.each do |user| %>
	<li>
		<%= gravatar_for_user user , :size => 30 %>
		<%= link_to user.name , user_path(user.id) %>
	</li>
	<% end %>
</ul>
<%= will_paginate @users %>
