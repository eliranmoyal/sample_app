<%= form_for @micropost do |f| %>
<%= render 'shared/error_messages' , :object => f.object %>
<div class="field">
	<%= f.label :content %> </br>
	<%= f.text_area :content %>
</div>
<div class="action">
	<%= f.submit "Create" %>
</div>				
<% end %>